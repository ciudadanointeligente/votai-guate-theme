============
Installation
============

At the command line::

    $ easy_install votai-theme

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv votai-theme
    $ pip install votai-theme
